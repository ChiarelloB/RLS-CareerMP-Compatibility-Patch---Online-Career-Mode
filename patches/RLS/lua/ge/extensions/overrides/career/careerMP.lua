-- CareerMP loads the multiplayer career entrypoint as `career_careerMP`.
-- Reuse the RLS-overhauled career implementation for that extension too.
return require('lua.ge.extensions.overrides.career.career')
